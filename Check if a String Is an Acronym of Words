class Solution {
public:
    bool isAcronym(vector<string>& words, string s) {
        string res;
        for(int i=0;i<words.size();i++)
        {
            int c=words[i][0];
            res+=c;
        }
        if(res==s) return true;
        else return false;
    }
};
